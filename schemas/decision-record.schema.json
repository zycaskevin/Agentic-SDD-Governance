{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SDG Decision Record",
  "type": "object",
  "required": ["decision_id", "risk_level", "summary", "scope", "basis", "status", "recorded_at", "reopen_condition"],
  "properties": {
    "decision_id": {"type": "string", "minLength": 1},
    "risk_level": {"enum": ["L2", "L3"]},
    "summary": {"type": "string", "minLength": 1},
    "scope": {"type": "string", "minLength": 1},
    "basis": {"type": "string", "minLength": 1},
    "status": {"enum": ["approved", "completed", "superseded"]},
    "recorded_at": {"type": "string", "format": "date-time"},
    "reopen_condition": {"type": "string", "minLength": 1},
    "assumptions_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "assumptions": {"type": "array", "minItems": 1},
    "operation_id": {"type": "string", "minLength": 1},
    "approved_by": {"type": "string", "minLength": 1},
    "expires_at": {"type": "string", "format": "date-time"},
    "consumed_at": {"type": ["string", "null"], "format": "date-time"},
    "approval_nonce": {"type": "string", "minLength": 12},
    "receipt_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "operation_payload_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "signature_algorithm": {"const": "ed25519"},
    "approval_envelope": {"type": "object"}
  },
  "allOf": [
    {
      "if": {"properties": {"risk_level": {"const": "L2"}}},
      "then": {
        "required": ["assumptions", "assumptions_sha256", "approved_by", "expires_at", "approval_nonce", "receipt_sha256", "signature_algorithm", "approval_envelope"]
      }
    },
    {
      "if": {"properties": {"risk_level": {"const": "L3"}}},
      "then": {
        "required": ["operation_id", "approved_by", "expires_at", "consumed_at", "approval_nonce", "receipt_sha256", "operation_payload_sha256", "signature_algorithm", "approval_envelope"],
        "allOf": [
          {
            "if": {"properties": {"status": {"const": "approved"}}},
            "then": {"properties": {"consumed_at": {"type": "null"}}}
          },
          {
            "if": {"properties": {"status": {"const": "completed"}}},
            "then": {"properties": {"consumed_at": {"type": "string", "format": "date-time"}}}
          }
        ]
      }
    }
  ],
  "additionalProperties": false
}
