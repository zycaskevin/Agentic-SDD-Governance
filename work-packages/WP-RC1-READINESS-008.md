# Work Package: RC1 Developer Experience and Production Readiness

## References

- Issue: `#22`
- Baseline: `main` at `1a5a0b214eccc2b9edd076fd5e2f222c4a456725`
- SDD: `docs/AUTONOMOUS_DEVELOPMENT_V1_2.md`, `docs/HARD_GATES_V1_2.md`, `docs/EVIDENCE_DRIVEN_SDD.md`, and `docs/CI_COST_GUARD.md`
- Risk: L2 because R22 changes the public approver-authority source from a caller-selected external root-owned path to one fixed root-controlled path; all other R22 engineering is bounded L1, while public TestPyPI/PyPI publication, real Owner key provisioning, `/etc` writes, and root service installation remain separate external actions
- Evidence: `DEP-RC1-REVIEW-FIX-R22-030` (authoritative); `DEP-RC1-REVIEW-FIX-R21-029`, `DEP-RC1-REVIEW-FIX-R20-028`, `DEP-RC1-REVIEW-FIX-R19-027`, `DEP-RC1-REVIEW-FIX-R18-026`, `DEP-RC1-REVIEW-FIX-R17-025`, `DEP-RC1-REVIEW-FIX-R16-024`, `DEP-RC1-REVIEW-FIX-R15-023`, `DEP-RC1-REVIEW-FIX-R14-022`, `DEP-RC1-REVIEW-FIX-R13-021`, `DEP-RC1-REVIEW-FIX-R12-020`, `DEP-RC1-REVIEW-FIX-R11-019`, `DEP-RC1-REVIEW-FIX-R10-018`, `DEP-RC1-REVIEW-FIX-R9-017`, `DEP-RC1-REVIEW-FIX-R8-016`, `DEP-RC1-REVIEW-FIX-R7-015`, `DEP-RC1-REVIEW-FIX-R6-014`, and `DEP-RC1-REVIEW-FIX-R5-013` are predecessor packages.

## Objective Contract

- Outcome: make the first SDG experience understandable in 30 seconds, prepare a safe `0.2.0rc1` distribution path, close the known redaction and L3 operational-readiness gaps, document recovery, and measure Monorepo rollback performance without weakening exact-tree verification.
- Success metric: the demo is deterministic; English and Traditional Chinese onboarding are materially equivalent; fast and controlled installs are tested; release artifacts pass fresh-wheel smoke tests; L3 readiness is diagnosable; redaction is bounded and cross-chunk safe; recovery documentation is executable; and the Monorepo benchmark reports reproducible measurements.
- Keep condition: Candidate code never becomes its own trusted verifier; no mock or break-glass path grants production authority or reports a false Gate PASS; no raw Evidence, private key, credential, or real user data enters the repository.
- Rollback condition: revert this Work Package if it weakens fail-closed action classification, trusted-Base verification, redaction, exact-tree rollback proof, key separation, or hosted CI cost controls.

## Executable Scope

1. Add a one-command, synthetic 30-second demo that exposes the strongest allow/block/redact properties without using real data or network access.
2. Align the English README with the Traditional Chinese Profile, L0-L3, installation, lifecycle, and limitations content; document fast and controlled installation paths.
3. Prepare version `0.2.0rc1`, a separate TestPyPI/PyPI Trusted Publishing workflow, attestations, build validation, and a fresh-wheel Codex/Hermes smoke test.
4. Document Owner key ceremonies per trust domain, rotation, revocation, backup, and loss recovery; provide root-managed Linux/macOS Broker service assets plus an L3 readiness diagnostic that never consumes a real nonce.
5. Bound redaction input size before allocation and implement deterministic streaming text redaction that preserves matches spanning read chunks.
6. Document the required single rollback commit, squash workflow, and an out-of-band break-glass incident path that never bypasses or relabels a failed Merge gate.
7. Add a reproducible Monorepo rollback benchmark and preserve full-tree equality unless measurements prove a safe optimization is required.
8. Keep canonical, packaged, and installed Governance resources synchronized where protected documentation or policy references change.
9. Close the PR #29 review findings: descriptor-bind release inputs, preserve Broker service availability after transient accept failures, reject ambiguous CI exemptions, close Windows-path and partial-key-marker redaction gaps, and separate proof correctness from benchmark latency decisions.
10. Close the PR #30 review findings: bind release directory generations, bound collector input before buffering, recursively scan shareable Evidence, validate exact service hardening, reject stale CI exception mappings, and align release/report/runbook contracts without fabricating the independent Review receipt.
11. Close the PR #32 exact-Head review findings: make the empty-capability Linux service viable, enforce exact Broker socket and ledger-capacity contracts, reject unknown CI exemptions, preserve primary release failures, declare release-helper platform scope, reconcile Redaction publications after source mutation, bound launchd restart rate, and correct unsupported predecessor Evidence wording without reclassifying autonomous development as an L3 operation.
12. Close the PR #33 exact-Head review findings: consume complete Broker connection framing before any state change, reject near-capacity appends before nonce-set allocation, fix the approver authority path, launchd group/umask/executable/log-retention contracts, platform-guard offline bundles, make release-job Python deterministic, remove the unused runtime packaging dependency, clarify inert rollback tooling preconditions, and remove unsupported predecessor Evidence claims.
13. Close the independent PR #34 review findings: keep every commit between the atomic rollback reference and reviewed Head inside the trusted Base's `evidence/**` audit allowance, defer all Merge Gate commits until after reviewed Head, and install both hash-locked governance and release requirements in the isolated workflow environment that executes the complete test suite.
14. Close the independent PR #36 review finding and valid CodeRabbit findings: publish the Broker socket from an owner-only descriptor-anchored staging directory with native atomic no-clobber semantics on Linux and macOS; prove real AF_UNIX health, signal cleanup, replacement preservation, and restart behavior; inspect every audit-window commit parent-to-child so a later restoration cannot hide an illegal intermediate change; reject a ledger exactly at capacity; and align current provenance and operator documentation.
15. Close the independent PR #38 review findings: sanitize bare and space-containing local paths in every public release error, catch compressed-archive failures, write reports only through descriptor-bound no-follow operations, and align the current authoritative DEP and Gate with this Work Package's L1 classification without rewriting predecessor Evidence.
16. Close the independent PR #39 review findings: enumerate every commit reachable in post-review and rollback audit windows, bound every untrusted DEP read, publish Pilot reports through descriptor-bound no-follow operations, extend local-path redaction to temporary and space-containing paths, withdraw affected legacy shareable artifacts through forward-only errata, suppress raw benchmark exceptions, require explicit L3 break-glass approval, route bug work through the Governance Skill, and harden fragmented Broker health and native-test diagnostics.
17. Close the independent PR #40 review findings: make failed Pilot report creation reuse the descriptor-relative, generation-safe cleanup transaction so a writer that replaces the pathname during cleanup is preserved rather than deleted by a stat-to-unlink race; publish Monorepo benchmark reports through the same exclusive no-follow writer with generic bounded write failures; canonicalize only Darwin's platform-owned `/tmp` and `/var` aliases through a shared safe normalizer while exercising Pilot/demo and full fresh-wheel output on hosted macOS; redact canonical Darwin plus WSL-mounted and WSL-UNC user paths in core and release reports; make product, native, and installed-wheel Broker health clients share the same EOF-aware, seven-byte, monotonic-deadline contract; scope every legacy Evidence withdrawal exemption to the exact report index and registered manifest artifact; and keep CodeRabbit under its 300-file limit by excluding only immutable predecessor Evidence through a protected transparent configuration.
18. Close the independent and CodeRabbit PR #41 review findings: redact native Windows WSL UNC paths for both `wsl.localhost` and `wsl$`; make every multi-file redaction call transactional across all prior publications; reuse the shared Darwin `/tmp` and `/var` alias normalizer in Evidence collection; run the complete source suite and repository validation before native workflow packaging; drain installed-Broker pipes while waiting for shutdown; require Python 3.10 or newer in the demo; and keep only immutable R6-R19 Evidence outside the current automated review set.
19. Close the independent and CodeRabbit PR #42 review findings: preserve transactional redaction outputs across descriptor-close failures and fail closed on newline-split sensitive fields; redact native and JSON-escaped WSL UNC paths; perform zero pathname operations when a trusted output dirfd is supplied; make regular-file, release-helper, and Broker socket cleanup atomically claim generations before deletion while supporting near-`NAME_MAX` leaves and preserving later writers; bind staged redaction outputs, Evidence control documents, and attachments to their original identity, size, and digest before publication; pin readable control-file modes and a concrete clock-rollover deadline; revalidate the full public release inventory after every download; keep native source Green before packaging; and retain short-path fresh-wheel fallback plus bounded structured diagnostics.
20. Close the independent and CodeRabbit PR #43 review findings: make every Evidence mutation commit only after its retained root/path leases and complete generation snapshots pass, preserve later writers through collision-safe no-clobber cleanup, make release and Broker descriptor/signal cleanup drain every resource without masking the primary failure, run the TestPyPI byte-equality verifier with the source package import path, reject ambiguous root/distribution release names, keep macOS hostile-file tests portable, correct current documentation and provenance, and bind the fixed approver-authority contract plus its separated `sddgov-owner` approval client to the Owner-signed L2 decision `DEC-RC1-APPROVER-AUTHORITY-R22` without performing the separate privileged provisioning action.

## Acceptance Tests

- `demo/run.sh` completes with a concise PASS transcript in an offline synthetic temporary directory and fails nonzero when any demonstrated invariant is false.
- English README includes the Profile and L0-L3 tables, fast trial install, controlled verified install, first demo, managed files, lifecycle commands, and experimental limitations.
- Package metadata reports `0.2.0rc1`; built wheel and sdist pass metadata checks; a fresh virtual environment installs the wheel and passes version, `validate`, Codex/Hermes `setup-agent`, `doctor`, and offline pilot checks.
- The publish workflow is separate from `pull_request_target`, uses least-privilege permissions, a protected environment, OIDC Trusted Publishing, pinned actions, TestPyPI before PyPI, and default attestations.
- L3 readiness reports structured checks for runtime context, fixed socket path, root-owned non-writable parent chain, socket ownership/mode/type, non-root Agent identity, and a bounded non-consuming health handshake; normal install Doctor remains valid without L3.
- The approver trust source rejects caller-selected environment routing, retains and rechecks the complete root-owned non-writable `/etc/sddgov/trusted-approvers.json` and `/etc/sddgov/trusted-approver-domains.json` path generations, keeps the receipt and approver record readable by the exact trusted Base, and prevents current-verifier reuse outside the externally bound canonical repository and trust domain.
- The Agent-side CLI can validate and display an L2 card but cannot sign it; the separate Owner CLI derives canonical assumptions, signer ID, validity, and the reviewed client source digest from the request, requires that client binding exactly once in the signed decision artifact, accepts only one terminal A/B choice, rejects terminal-control text and Python import injection, uses only one matching confirmation-constrained Ed25519 SSH-agent identity, verifies the returned signature against the fixed key and audience stores, and creates no receipt for refusal or signer failure. `sddgov decision verify-product` must then reverify the stored signature, row, active key, audience, expiry, exact request scope, assumptions, client binding, and validity; a schema-shaped row alone is never Green.
- Broker service assets install no credentials or keys and their verification tests run without performing a live privileged installation.
- Native Linux and macOS tests exercise the installed Broker implementation with real AF_UNIX sockets; descriptor and pathname socket identities are explicitly treated as distinct, staging publication cannot overwrite the fixed final path, and cleanup never removes a replacement socket.
- Redaction rejects inputs over the trusted limit before unbounded allocation and detects credentials/private keys whose matches cross internal read chunks; symlink, hardlink, FIFO, transactional, and report integrity guarantees remain Green.
- Recovery documentation clearly distinguishes preparation, authorized operational recovery, incident evidence, and Gate PASS; no caller-controlled override flag is introduced.
- The Monorepo benchmark records fixture size, Git version, elapsed time, return status, and exact-tree result; any optimization must retain an independent full-tree equality assertion.
- Full unit tests, repository validation, CI Guard, Local Green, canonical/package/install parity, package build, and fresh-wheel smoke tests pass.
- Release hashing, archive parsing, and copies consume one descriptor-bound input generation and fail closed on mutation or pathname replacement; transient GitHub API retries remain bounded to connection and 5xx failures.

## Non-scope and External Boundaries

- Do not publish to TestPyPI or PyPI, create a public GitHub Release, modify GitHub environments/rulesets, or push credentials without the required external authorization and working repository authentication.
- Do not generate, import, reveal, or store a real Owner private key in the repository, chat, Evidence, CI, or Agent workspace.
- Do not install a root service or write `/etc/sddgov`, `/run/sddgov`, or `/private/var/db/sddgov` during autonomous development.
- Do not add a production `--mock-broker`, caller-selected Broker path, rollback bypass, affected-path-only proof, or automatic force-push recovery.
- Do not claim empirical superiority over other governance systems from fixture-only results.

## Verification Plan

- Preserve a Red reproduction for unbounded redaction input and missing L3 operational diagnostics before implementation.
- Add targeted unit and integration tests for every new command, parser, file-boundary, workflow contract, documentation invariant, and benchmark result schema.
- Run the original Red checks, focused suites, full Local Green, package build, and a fresh-wheel smoke test.
- Before package construction, run `PYTHONPATH=src .governance-venv/bin/python -m sddgov.cli validate .`; only a successful source validation may proceed to `python -m build --no-isolation` and fresh-wheel verification.
- Complete and strictly verify `DEP-RC1-REVIEW-FIX-R22-030` at L2; import only an Owner-signed Ed25519 receipt for `DEC-RC1-APPROVER-AUTHORITY-R22`, and generate a local PR attachment block only from redacted shareable artifacts. R22 records the independent and CodeRabbit PR #43 reviews and corrects R21's inaccurate statement that every related test had already executed. R21 records the independent and CodeRabbit PR #42 reviews that rejected incomplete descriptor-close transactions, cleanup claim races, cross-line and escaped-UNC redaction gaps, control-plane operational ambiguity, repeated-download provenance, and unbound attachment staging. R20 records the independent and CodeRabbit PR #41 reviews that rejected the incomplete WSL-UNC boundary, non-transactional multi-file redaction, Darwin Evidence alias gap, and validation-after-package ordering. R19 records the independent PR #40 review that rejected the Pilot report failure-cleanup TOCTOU; R18 records the independent PR #39 review, the forward-only legacy Evidence withdrawal/errata registry, and fresh proof after the historical package-order corrections. R17 records the independent PR #38 review that rejected the public release-report boundary and current L1/L2 metadata mismatch; R16 records PR #37's native Darwin path-boundary failure and proves native Linux/Darwin execution from an installed wheel; R15 records the independent PR #36 review that rejected the Broker's FD/path inode assumption; R14 records the predecessor release, cleanup, and macOS logging remediation; R13 records the independent PR #34 review that rejected the rollback topology and isolated release-test environment. Earlier packages remain immutable predecessor evidence and are not rewritten. A collector UUID or redacted numeric review suffix is not used as GitHub identity.
