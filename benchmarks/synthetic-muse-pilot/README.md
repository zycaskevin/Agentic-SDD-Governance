# Synthetic Muse Isolated Pilot

This pilot installs the current SDG bundle into a fresh disposable Hermes project and uses synthetic relationship text plus one generated 1x1 image. It performs no network call and uses no real person, relationship, image, account, credential, or Production data.

The binary image is expected to fail closed at the Local Redaction Gateway. A reviewed text metadata derivative then completes `Red -> Evidence -> Fix -> Green -> Proof`, full local strict verification, and portable PR verification.

Run:

```bash
sddgov pilot synthetic-muse --output benchmarks/synthetic-muse-pilot/latest-result.json
```

`PASS` proves only this bounded synthetic pilot and the included adversarial tests. It does not authorize Muse Production use, real-data access, or L2/L3 operations.
