# Baseline Provenance

The canonical GitHub repository is `zycaskevin/Agentic-SDD-Governance`. Its `main` branch was verified on 2026-08-08 at commit `402517808d22c3daa44811301e96c7964db3dc5a`. That Initial commit contains only the Apache License 2.0 `LICENSE` file. The commit and license are preserved in the local delivery history.

The earlier ChatGPT deliverable was identified as `agentic-sdd-governance-v0.1.0.zip` / `v0.1.0-draft`. The conversation record confirms that it contained 56 source files, a Python CLI with `init`, `validate`, and `status`, governance/adapters/templates, and three passing tests.

The historical attachment endpoint remained visible in the owner's signed-in conversation, but automated retrieval was rejected by the expired/protected download path. No byte-for-byte v0.1 source archive was therefore available inside this Work workspace, and the current GitHub baseline does not contain those v0.1 files.

`v0.2.0-experimental` preserves the actual GitHub baseline and adds a clean canonical reconstruction of the verified v0.1 capabilities plus the approved v0.2 governance/evidence plan. It must not be described as a textual patch against every original v0.1 file.

No BugEzy source code, private material, or third-party repository files were copied into this version. Evidence capture, DEP schemas, CLI behavior, templates, and redaction rules are independently implemented from the approved product requirements.
