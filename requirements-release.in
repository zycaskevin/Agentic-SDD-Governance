# Trusted release-tool inputs. Regenerate requirements-release.lock with:
# uv pip compile requirements-release.in --generate-hashes --python-version 3.12 --output-file requirements-release.lock
build>=1.3,<2
packaging>=26,<27
setuptools>=80,<81
twine>=6.2,<7
wheel>=0.45,<1
