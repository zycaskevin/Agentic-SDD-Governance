# Fix Scope

## Smallest sufficient change

Close the verified R10 review boundaries with bounded reads/retries, exact release and package contracts, synchronized protected-resource/docs behavior, accurate versioned Evidence metadata, and targeted regression tests. Preserve all product changes in one atomic commit directly above Base.

## Files or components in scope

`src/sddgov/evidence.py`, `src/sddgov/broker.py`, `src/sddgov/cli.py`, `src/sddgov/__init__.py`, release and benchmark scripts/workflow, dependency metadata/lock, demo and Traditional Chinese onboarding, protected-file policy mirrors, Work Package sequencing/authority, predecessor audit clarifications, and their focused repository/unit tests.

## Explicit non-scope

No TestPyPI/PyPI/GitHub Release publication; no GitHub environment/ruleset mutation; no real Owner key; no root Broker installation; no WSL2/macOS privileged rehearsal; no mock Broker, rollback bypass, affected-path-only proof, force push, review-thread mutation, or fabricated independent receipt. No broad docstring-only rewrite is included.

## Blast radius

The changes touch release supply chain, Evidence verification, L3 Broker availability, protected-file review scope, packaging, and public onboarding. Risk is bounded by fail-closed tests, exact canonical/packaged/installed parity, source-before-build validation, fresh-wheel isolation, and a real Base rollback rehearsal.
