# Regression Evidence

## Regression test added or strengthened

Added quoted/unquoted `wsl.localhost` and `wsl$` fixtures with spaces and five-byte streaming chunks; multi-source symlink/disappeared/open failures; later-writer preservation at call-wide cleanup; Darwin temporary-system collection; source-before-build ordering; unsupported venv-Python fallback; real output above a pipe buffer; and canonical withdrawal ordering.

## Related tests executed

The dual-lock full suite passed 410 tests with 13 expected local sandbox/native skips. Local Green repeated the same suite and Validate. Real Linux AF_UNIX tests passed 8 with one expected installed-wheel provenance skip.

## Unaffected paths sampled

CI permission rules, exact Broker framing, release bundle integrity, installer resource parity, legacy Evidence withdrawal scoping, rollback verifier tests, Codex/Hermes setup and Doctor, and the offline quick demo all passed.
