# Fix Scope

## Smallest sufficient change

Add native WSL UNC alternatives to the shared local-path detector; retain all successful publications until a multi-file call commits; reuse the fixed Darwin alias normalizer in Evidence; place full source tests and Validate before build; use `communicate` for Broker shutdown; and close the three valid minor findings.

## Files or components in scope

`src/sddgov/redaction.py`, `src/sddgov/evidence.py`, native/release workflows, fresh-wheel smoke, offline demo, mirrored redaction guidance, the installed manifest, Work Package provenance, CodeRabbit scope, and focused tests.

## Explicit non-scope

No release publication, root Broker installation, production data, Owner key, approval semantics, caller-controlled path canonicalization, or historical Evidence rewrite.

## Blast radius

Cross-platform path redaction, Evidence collection, release/native CI ordering, and installed-wheel smoke are affected. Rollback is one atomic commit and restores the exact Base product tree while audit Evidence remains.
