# Regression Evidence

## Regression test added or strengthened

Added or strengthened tests for release `total_count` mismatch, user-directory expansion, exact installed `validate`, shared protected release helpers, no-EOF Broker requests, Broker request size, real approver-store schema/key/repository boundaries, portable real path chmod, stable monotonic clocks, shared nonce validation, specific CI negative errors, partial private-key delimiter fail-closed behavior, managed governance validation, synchronized Broker runbooks/services, and source-checkout demo binding. The nine R4 nitpicks are also covered by refactoring or runbook assertions.

## Related tests executed

The exact implementation clone ran 278 tests with 2 environment skips and no failures. Source repository and installed managed-tree validation, CI Guard, Doctor, source demo, Local Green, wheel/sdist build, twine, offline bundle inventory, and fresh-wheel Codex/Hermes setup/Doctor/validate/pilot all passed. The source demo completed five synthetic checks in 0.20 seconds.

## Unaffected paths sampled

Autonomy L0-L3 receipt and runtime-context tests, Evidence symlink/hardlink/FIFO/transaction/integrity tests, trusted-Base Merge verification, independent Reviewer signing, installer lifecycle, CI permission controls, exact-tree monorepo benchmark claim boundary, and package resource parity remained Green. A disposable revert returned exactly to the Base tree, experimental.8 Doctor passed with 66 managed files, and 229 Base tests passed with the same 2 environment skips.
