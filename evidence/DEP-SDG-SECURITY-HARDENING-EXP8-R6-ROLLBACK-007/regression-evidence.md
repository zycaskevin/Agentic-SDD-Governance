# Regression Evidence

## Regression test added or strengthened

Permanent tests cover Base-only v1, standalone unknown text, `HEAD`, short/nonexistent/out-of-range refs, and canonical full-SHA candidate-range acceptance.

## Related tests executed

RED targeted tests captured. Targeted Merge/Reviewer tests, full suite, validate, CI verify, Local Green, strict DEP, package, doctor, and pilot remain pending after implementation.

## Unaffected paths sampled

Authority, Evidence transactions, CI YAML semantics, redaction, external actions, and L3 broker remain outside the code change and will be sampled by the full suite.
