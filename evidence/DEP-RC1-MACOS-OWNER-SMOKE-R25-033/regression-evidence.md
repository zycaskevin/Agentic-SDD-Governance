# Regression Evidence

## Regression test added or strengthened

The fresh-smoke workspace context now returns the canonical fixed Darwin alias before any derived path is created. A new POSIX test patches the platform to Darwin and requires a workspace created under `/tmp` to be returned under `/private/tmp`. The existing fallback test remains Green.

## Related tests executed

The two focused workspace tests pass. The candidate-plus-Evidence suite ran 524 tests with 14 expected platform/sandbox skips and no failures. In the externally bound canonical repository, configured Local Green ran the same 524 tests with 5 expected skips, repository validation, and exact stored product-decision verification; every command returned zero.

## Unaffected paths sampled

The eight-file Owner client identity remains `ac944efc040cbc103cd1664ebbd365378539f66011a92613b1c2200ef1ead826`, matching the already signed request. No Owner source, trust loader, signer path, receipt, or runtime custody check changed. An isolated release-tool environment built and checked both distributions, and a no-checkout-import installed-wheel smoke passed the offline bundle, Owner client, Codex/Hermes 73-file Doctor/validation, demo, and native Linux Broker checks.
