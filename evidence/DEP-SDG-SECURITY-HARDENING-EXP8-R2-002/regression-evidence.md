# Regression Evidence

## Regression test added or strengthened

Added closed-envelope authority cases; exact Draft-condition, YAML-key, runner/concurrency, workflow symlink/hardlink cases; real FIFO timeout; collect/redact/transition pre/post-publication and later-writer cases; same-inode exact-byte attachment mutation; link-boundary attachment mutation; and declarative rollback rejection.

## Related tests executed

Targeted autonomy, CI, Evidence, Merge, Reviewer, Redaction, and repository-contract suites pass. The complete local suite passes with one expected root-only L3 identity skip.

## Unaffected paths sampled

Normal Red → Evidence → Fix → Green → Proof flow, clean zero-match redaction, durable Operational Action state, L2/L3 receipt verification, full repository tests, and packaged canonical-copy equality remain Green locally.
