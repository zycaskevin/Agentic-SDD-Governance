# Root Cause Hypothesis

## Hypothesis

The candidate treated incomplete heuristics and metadata checks as proof. Low-risk classification still attempted to infer authority from caller-controlled text; CI checked fragments rather than exact expressions and trusted pathname traversal; Evidence updated several files without a generation-bound publication protocol; attachment freshness used metadata rather than exact bytes; and rollback accepted arbitrary shell-shaped prose.

## Supporting evidence

The independent review evidence in `shareable/artifacts/terminal--independent-review-red.txt` lists the reproduced P0/P1 boundaries. Permanent tests reproduce target/parameter disguise, semantic CI bypass, workflow symlinks, FIFO input, publish-boundary failures, later-writer replacement, exact-byte control mutation, and rollback wrappers.

## Contradicting evidence

The initial 166-test suite, package build, doctor, and synthetic pilot were Green. This contradicts broad implementation failure but not the hypothesis: those checks lacked the newly identified hostile linearization and semantic cases.

## Falsification test

Replace heuristics with a closed authority-free L0/L1 envelope; require exact semantic CI contracts and safe dirfd reads; add nonblocking type checks; introduce expected-generation no-clobber control publication; re-read exact control bytes at attachment linearization; and replace rollback strings with a declarative allowlist. Each former bypass must then fail while ordinary L0/L1 work and the normal DEP flow remain Green.

## Conclusion

Confirmed. The bounded fixes and hostile regressions implement the falsification test locally. Fresh independent review remains the separate proof gate.
