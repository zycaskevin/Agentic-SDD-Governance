# Fix Scope

## Smallest sufficient change

Replace the invalid identity assumption with descriptor-anchored staging and
native no-clobber publication, then exercise the real server path on each
supported OS without weakening root, reviewer, trusted-Base, or rollback gates.

## Files or components in scope

- Broker publication/cleanup core and exact ledger-capacity boundary.
- Native Linux/Darwin CI and installed-wheel Broker smoke.
- Real AF_UNIX regression tests without class-level identity/socket mocks.
- Per-commit-edge merge and rollback audit-window enforcement.
- R15 Work Package/operations documentation and sanitized proof paths.

## Explicit non-scope

No package publication, root service installation, production authority store,
Owner/reviewer key creation, protected-environment change, historical Evidence
rewrite, runtime Agent integration, or benchmark superiority claim.

## Blast radius

L3 service availability, filesystem publication races, hosted CI, merge-history
validation, release smoke, and governance documentation. Application code and
public data formats remain unchanged.
