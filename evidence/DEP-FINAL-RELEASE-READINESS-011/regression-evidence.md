# Regression Evidence

## Regression test added or strengthened

Added permanent tests for three classifier exit codes; risk/ID/scope/L3 payload binding; durable UAT; signed completion; forged local completion; unrelated Work Package continuation; first-consumer immutable policy; same-UID trust rejection; every tracked portable Proof DEP; and malformed CI exemptions.

## Related tests executed

216/216 tests passed with one sandbox-only Unix socket test skipped; the same socket contract was already covered by prior non-sandbox proof. Targeted autonomy, governance, CI, Merge Gate, Evidence, installer, repository-contract, Reviewer, and pilot suites passed.

## Unaffected paths sampled

Routine L0/L1 operations, L2 decision memory, L3 exact payload and broker, rollback reconciliation, Evidence path safety, review signing, Codex/Hermes setup, Doctor, and offline synthetic Muse pilot.
