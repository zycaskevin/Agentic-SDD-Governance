# Regression Evidence

## Regression test added or strengthened

`test_agent_loaded_governance_copies_are_protected_at_the_base` checks representative root, `.agents`, and `.agentic-sdd-governance` paths against Base-derived patterns. Repository-contract assertions require all canonical patterns.

## Related tests executed

Merge Gate protected-change fail-closed tests, repository-contract tests, and the complete suite are run after the policy update.

## Unaffected paths sampled

Existing canonical source, CI workflow, dependency-lock, Evidence, redaction, and trust-store paths remain protected.
