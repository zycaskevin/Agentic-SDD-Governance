# Regression Evidence

## Regression test added or strengthened

Add direct parser and full Merge tests proving exact legacy v1 passes, while duplicate keys, extra fields, wrappers, chaining, observational commands, alternate verification, and placeholders fail closed. Existing v2 tests remain Green.

## Related tests executed

Run Merge/Reviewer/repository-contract tests, full suite, validate, CI verify, Local Green, all strict portable DEPs, package build, fresh doctor, and synthetic Muse pilot.

## Unaffected paths sampled

Authority classification, Evidence transactions, redaction, L3 broker, protected installed copies, and hash-locked dependencies remain unchanged.
