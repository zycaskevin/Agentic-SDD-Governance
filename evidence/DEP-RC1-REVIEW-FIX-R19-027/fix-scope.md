# Fix Scope

## Smallest sufficient change

Close the eight independently reproduced R18 gaps without changing authority,
release version, DEP schema, or historical Evidence. Protect a CodeRabbit path
filter that excludes only immutable R6-R18 predecessor Evidence while keeping
product code and the current R19 DEP in automated review.

## Files or components in scope

Secure filesystem/redaction modules, Broker health readers, release and
benchmark report scripts, native workflow, protected policy mirrors,
`.coderabbit.yaml`, Work Package provenance, and focused contract tests.

## Explicit non-scope

No publication, GitHub Release, production deployment, root Broker install,
owner decision, reviewer signing, historical R6-R18 rewrite, general realpath
traversal, or weakening of arbitrary symlink rejection.

## Blast radius

Public reports, local-path redaction, Linux/WSL/Darwin paths, installed-wheel
Broker smoke, withdrawal verification, CodeRabbit selection, and release CI.
Rollback is one atomic product commit whose parent is exact Base.
