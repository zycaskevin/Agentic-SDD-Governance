# Verification

## Green command and result

Dual hash-locked inputs and `pip check` passed. Full tests and exact-interpreter
Local Gate each passed 403 tests with 12 sandbox-native skips; Validate and CI
verify passed. Outside the sandbox, seven real AF_UNIX tests passed with one
expected installed-wheel skip.

Wheel/sdist build and Twine passed. The bundle held 10 locked dependencies and
four public assets. Checkout-free smoke passed with 13 files/12 payloads,
Codex/Hermes Doctor at 71 managed files each, demo Green, and Linux Broker PASS.

Actual rollback cleanly reverted the atomic product commit, matched exact Base
outside audit paths, passed Base setup/Doctor/Validate and 229 tests with one
sandbox skip, built/Twine-checked Base artifacts, and passed a clean installed-
wheel consumer smoke.

## Before/after evidence

Red binds the independent R18 no-sign verdict. Green binds owned cleanup,
no-follow reports, exact aliases, Darwin/WSL masking, EOF-aware health,
artifact-exact withdrawals, native installed-wheel CI, and bounded review.

## Remaining limitations

Hosted Ubuntu/macOS checks, completed CodeRabbit review, exact trusted-Base DEP
and Gate verification, and a fresh independent Ed25519 receipt remain required.
No publication, production operation, root install, or private-key action ran.
