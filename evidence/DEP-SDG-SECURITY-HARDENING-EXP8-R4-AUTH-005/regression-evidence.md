# Regression Evidence

## Regression test added or strengthened

`test_l2_product_reuse_rejects_nested_decision_package` supplies a valid receipt and exact scope plus nested foreign operation data and requires a non-interactive BLOCKED result.

## Related tests executed

The full autonomy and repository suites are rerun, including exact package validation for genuine L2/L3 ACTION REQUIRED paths.

## Unaffected paths sampled

Top-level foreign-field rejection, exact L2 reuse without a package, L3 one-use approval, and durable Operational Action behavior remain covered.
