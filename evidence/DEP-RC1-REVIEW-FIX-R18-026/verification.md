# Verification

## Green command and result

After Red and implementation, the isolated Python 3.12 environment installed both hash-locked Governance and release inputs and `pip check` passed. `unittest discover -s tests -v` passed 392 tests with 12 expected sandbox/native skips. Source Validate, CI verify, and Local Gate passed; Local Gate resolved configured `python3` commands to the exact active environment interpreter.

Only after Green, the candidate wheel and sdist were built with `build --no-isolation` and both passed Twine. The offline bundle contained 10 locked dependency wheels and four public assets. Checkout-free installed-wheel smoke passed with 13 total bundle files, 12 payload files, Codex and Hermes Doctor at 71 managed files each, the synthetic demo Green, and a real Linux Broker health/cleanup cycle. The source native suite passed seven active real AF_UNIX tests with one expected installed-wheel provenance skip.

## Before/after evidence

Red binds the independent PR #39 FAIL/no-signature verdict and the exact valid findings. The replacement errata withdraws unsupported R6, R12, R13, and R14 claims without modifying predecessor bytes. Green binds complete reachable-commit traversal, mandatory Evidence byte ceilings, no-follow Pilot publication, expanded local-path masking, stable public benchmark errors, explicit L3 approval, Skill routing, and hardened health/diagnostic behavior.

## Remaining limitations

The sandboxed installed-wheel smoke failed closed only because the local sandbox denied AF_UNIX bind; its sanitized report is retained outside the DEP, and the identical sandbox-external run passed. Exact trusted-Base portable verification, the final digest/Gate, full isolated rollback, hosted Ubuntu/macOS installed-wheel proof, CodeRabbit completion, and a fresh independent Ed25519 receipt remain required before merge. No TestPyPI/PyPI publication, GitHub Release, root Broker installation, production operation, or private-key action is performed.
