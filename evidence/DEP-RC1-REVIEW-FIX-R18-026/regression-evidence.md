# Regression Evidence

## Regression test added or strengthened

- A two-commit side branch adds and removes a product path before merge; the Gate must still reject the hidden product commit.
- The Evidence reader's byte ceiling is mandatory at every call site, with oversized control and artifact regressions.
- Both Pilot report entry points preserve sentinel targets behind leaf and parent symlinks.
- Full-text and streaming redaction cover temporary, Darwin cache, and quoted space-containing paths while preserving a `/tmpfile` prefix sibling.
- Benchmark errors expose only a stable exception category; Broker health accepts fragmented responses; native diagnostics terminate or kill a stalled process before collection.
- Repository contracts bind explicit L3 approval, Skill-before-Red routing, exact withdrawal inventory, and a clean replacement artifact.

## Related tests executed

The isolated dual-lock Python 3.12 environment passed 392 tests with 12 expected local sandbox/native skips. The focused security suites passed 166 tests. Repository Validate, CI verify, and Local Gate passed. The real Linux AF_UNIX native suite passed seven active tests with one expected installed-wheel provenance skip.

## Unaffected paths sampled

Autonomy receipts, L3 nonce consumption, Broker staging publication, CI cost controls, installer/Doctor, package metadata, legacy manifest compatibility, reviewer signing, exact-tree rollback, every non-withdrawn predecessor DEP, and both Agent adapters remained Green.
