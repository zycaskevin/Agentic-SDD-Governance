# Verification

## Green command and result

The isolated Python 3.12 environment installed both hash-locked governance and
release inputs together. `pip check` passed. `unittest discover -s tests -v`
passed 385 tests with 12 expected local sandbox skips. Source Validate, CI
verify, and Local Gate passed. The focused release suite passed 23 tests.

## Before/after evidence

Red binds the PR #38 independent FAIL/no-signature verdict and its three
reproductions. Green binds boundary-aware masking, exclusive descriptor-bound
report publication, `zlib.error` handling, explicit R17 L1 provenance, exact
atomic topology, and the current full regression run.

## Remaining limitations

Candidate build and Twine checks passed. The deterministic offline bundle
covered 10 locked dependencies and four public assets. A checkout-free
fresh-wheel smoke passed with 13 inventoried files, Codex/Hermes Doctor at 71
managed files each, synthetic demo Green, and a real Linux Broker health/cleanup
cycle. An isolated `git revert --no-commit` of the R17 atomic commit produced an
exact Base product tree; Base setup-agent, Doctor, Validate, 229 tests, build,
and Twine checks passed.

Hosted Ubuntu/macOS installed-wheel native proof, trusted-Base portable
verification and digest, CodeRabbit completion, and a new independent Ed25519
receipt remain required before merge. Public TestPyPI/PyPI, GitHub Release,
root Broker installation, production state, and Owner/reviewer private-key
operations are not performed.
