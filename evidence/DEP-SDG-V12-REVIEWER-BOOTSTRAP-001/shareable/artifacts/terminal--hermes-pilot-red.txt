GB10 Hermes independent-review pilot, [REDACTED_PHONE_OR_NUMBER].

Observed:
- Reviewer reported an unclean checkout with untracked files, then continued instead of using a fresh clone.
- All 83 existing tests passed and the code/security review was otherwise positive.
- Reviewer did not create the required signed protected-file review receipt.
- Reviewer did not configure the public-key-only GitHub trust variable.
- Reviewer asked Arthur to provide a Reviewer ID and Ed25519 public key.
- Targeted command `PYTHONPATH=src python3 -m unittest tests.test_reviewer -v` failed before implementation with `ModuleNotFoundError: No module named 'sddgov.reviewer'`.

Expected:
- A separately assigned Reviewer creates and retains its own Repo-external identity.
- Public trust is registered machine-to-machine.
- A dirty checkout fails closed and routes to a fresh clone.
- The product owner is never used as a key or digest courier.
