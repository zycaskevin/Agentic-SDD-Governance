# Fix Scope

## Smallest sufficient change

Introduce a descriptor-owning regular-file abstraction for release tools and route validation, archive parsing, digests, and copies through it. Add bounded Broker accept recovery and GitHub API retry, strict CI exception validation, complete Windows/partial-marker redaction, closed-DEP ordering, runtime-module validation, exact Proof inventory checks, honest benchmark states, executable evidence ordering, and the accepted review-maintainability improvements.

## Files or components in scope

`scripts/release_files.py`, release bundle/fresh-wheel/environment/benchmark scripts, `src/sddgov/{broker,ci_guard,cli,evidence,redaction}.py`, publish workflow, Broker service and runbook, Traditional Chinese user guide, CI schema copies, managed resource copies, Work Package, and focused regression tests.

## Explicit non-scope

No TestPyPI/PyPI publication, GitHub Release, environment/ruleset mutation, Owner key creation, real L3 approval, root service installation, production `--mock-broker`, caller-selected rollback threshold, affected-path-only rollback proof, or review-thread reply/resolution.

## Blast radius

Release assembly and smoke input handling, Broker daemon availability, CI contract validation, evidence collection ordering, redaction, source validation, documentation, and synthetic benchmark reporting. Existing cryptographic receipt formats, Policy Kernel action classes, and production authority boundaries are unchanged.
