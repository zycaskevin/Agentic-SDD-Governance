# Fix Scope

## Smallest sufficient change

Reject contradictory machine-verifiable UAT without prompting; reserve exit 2 for a validated owner package and use 3 for parser/process errors; validate and recompute durable action identity before mutation; close both trusted policy marker paths; use one-child Traversable calls throughout bundled resource loading.

## Files or components in scope

`autonomy.py`, `cli.py`, `governance.py`, `merge_gate.py`, `schema_validation.py`, `installer.py`, External Action and autonomy policy Schemas, canonical/packaged/installed governance copies, docs/Changelog, and permanent unit/repository-contract tests.

## Explicit non-scope

No Production operation, credential, real data, GitHub security-setting mutation, new external vendor, Billing change, Hosted rerun, Merge, tag, or Release. Clean zero-match Evidence remains shareable after deterministic scanning.

## Blast radius

Classifier and CLI callers, durable Operational Action/Necessary UAT state, first-consumer Merge bootstrap, Python 3.10 package consumers, installed Governance Doctor, and release documentation.
